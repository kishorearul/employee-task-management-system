# Core framework (flexible pins: work on Python 3.12+ including newer interpreters)
fastapi>=0.116
uvicorn[standard]>=0.35
python-multipart>=0.0.20

# Database
SQLAlchemy>=2.0
alembic>=1.16
psycopg2-binary>=2.9
psycopg[binary]>=3.2

# Validation / settings
pydantic>=2.11
pydantic-settings>=2.10
email-validator>=2.3

# Auth - actively maintained equivalents (see README / interview docs):
# PyJWT instead of unmaintained python-jose, pwdlib/bcrypt instead of unmaintained passlib
PyJWT>=2.10
bcrypt>=4.3
pwdlib[bcrypt]>=0.2

# Testing
pytest>=8.4
httpx>=0.28

# Utilities
python-dotenv>=1.1
